# Optional: require this header on the OpenAPI skill routes (/api/*).
# Foundry custom-OpenAPI-tool registration requires declaring at least one
# auth scheme, so we expose a simple API-key header. For the POC you can
# leave this unset to allow any caller.
TOOL_API_KEY=
