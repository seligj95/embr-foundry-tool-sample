platform: python
platformVersion: "3.12"
autoDeploy: true

run:
  port: 8000
  startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 8000"

healthCheck:
  enabled: true
  path: "/health"
  expectedStatusCode: 200
